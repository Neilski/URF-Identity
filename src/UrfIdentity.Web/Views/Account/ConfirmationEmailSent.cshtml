@using Identity.Resources
@model Identity.UI.ViewModels.Account.ConfirmationEmailSentViewModel


@{
    ViewBag.Title = AccountResources.ConfirmationEmailSent_Title;
}

<h2>@ViewBag.Title</h2>

<div>
    @Html.Raw(String.Format(AccountResources.ConfirmationEmailSent_Message, Model.Email))
</div>